#
#   Linux commands for csv files
#
# head -n -1 <name>  cat the 1 line of CSV
#tail -n 2   <name>  cat the last 2 line of CSV
#awk                 print columns of CSV 

python csvr.py   and then enter  xxxx.csv
